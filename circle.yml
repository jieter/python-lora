dependencies:
  post:
    - pyenv install --list
    - pyenv local 2.7.11 3.4.3 3.5.0 3.6.3
test:
    pre:
        - sudo apt-get update -qq; sudo apt-get install python3.4-dev
    override:
        - tox
